#!/usr/bin/env python3

# import sys
# from tests import database, collector, utils

# verbose = True if len(sys.argv) > 1 and sys.argv[1] == '-v' else False

# collector.TestbedMoteCollectorTest(verbose=verbose).test()